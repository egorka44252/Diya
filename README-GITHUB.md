# Public GitHub build

This folder is a browser-ready public build. The application JavaScript is delivered through an obfuscated loader instead of readable source files.

Important: anything required by a browser can still be downloaded by a determined visitor. This build is a deterrent against casual source copying, not cryptographic protection.

For real secrecy, keep the original source in a private repository and expose only this build publicly.
